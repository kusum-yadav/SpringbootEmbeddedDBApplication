CREATE TABLE ADDTOCART (ID INT PRIMARY KEY AUTO_INCREMENT, COUNT INT,DESCRIPTION VARCHAR(255),PRICE INT, PRODUCT_NAME VARCHAR(255), RETURNABLE BOOLEAN,SOLD_BY VARCHAR(255) );
CREATE TABLE ADDTOWISHLIST(WISH_ID INT PRIMARY KEY AUTO_INCREMENT,PRICE INT,PRODUCT_NAME VARCHAR(255),QUANTITY INT,REPLACEMENT BOOLEAN,RETURNABLE BOOLEAN);